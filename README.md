# R Course Quarto Project

Render the full book:

```bash
quarto render
```

Open the HTML entry at `_book/index.html`.

If you lack SAS files in `data/sdtm/`, the book synthesizes small demo data so every chapter still runs.
"# r4sas" 
